/* Stub notification helper (notificationHelper.js) */

export const sendAppointmentNotifications = async ({ appointment, io, action }) => {
  console.log("notificationHelper: sendAppointmentNotifications called (stub) with action:", action, "appointment:", appointment);
  // (Stub implementation – replace with actual notification logic as needed)
  return Promise.resolve();
}; 